package cn.i.learn.scala.base.tuple

object Tuple2List {
  def main(args: Array[String]): Unit = {
    val tuple=("a", "b", "c")
    tuple.productIterator.toList.foreach(println)
    tuple.productIterator.toSeq.map(x=>x.toString()).foreach(println)
  }
}