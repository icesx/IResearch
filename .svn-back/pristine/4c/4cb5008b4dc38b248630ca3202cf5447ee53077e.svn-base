package cn.i.learn.scala.clazz

import org.apache.log4j.Logger

object CaseClass {
    private val logger: Logger = Logger.getLogger(CaseClass.getClass)
    private case class Persion(nama: String)
    def main(args: Array[String]): Unit = {
        println(Persion("name"))
    }
}