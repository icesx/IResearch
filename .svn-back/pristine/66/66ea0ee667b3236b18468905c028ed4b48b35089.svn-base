package cn.i.learn.scala.base
import collection.breakOut
object SetTest {
    def main(args: Array[String]): Unit = {
        val map: Map[String, String] = Set("1", "3", "2").map(i => i -> i)(breakOut)
        println(map)
        println(Set("1", "3", "2").map(i => i -> i)(breakOut))
        println(Set("1", "3", "2").map(i => i -> i))
    }
}