package cn.i.xportal.jdp.strategy.sortarray;

import org.apache.log4j.Logger;

public class RadixSort extends SortStrategy {
	private static Logger logger = Logger.getLogger(RadixSort.class);
	public void sort() {
		logger.info(this);
	}
}
