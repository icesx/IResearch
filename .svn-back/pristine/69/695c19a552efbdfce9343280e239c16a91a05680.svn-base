package cn.i.learn.scala.list

object ListToMap {
    def main(args: Array[String]): Unit = {
        println((1 to 10).toList.map(x => Map(x -> x)))
        println((1 to 10).toList.map { x => x -> x })
        println((1 to 10).toList.map(x => x -> x))
    }
}